import PogObject from "../../PogData";

// Unused as of now
export const data = new PogObject("IQ", {
    supplytimes: {
        x: Renderer.screen.getWidth() / 2,
        y: Renderer.screen.getHeight() / 2 + 10,
        scale: 1
    },
    lifelinedisplay: {
        x: Renderer.screen.getWidth() / 2,
        y: Renderer.screen.getHeight() / 2 + 10,
        scale: 1
    },
},"data.json");

export default data 